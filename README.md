# VitaminCare Android-Style Prototype

A polished Streamlit prototype styled like a mobile health app.

Flow:
Splash/Login → Home Dashboard → Assessment → Results → Reports → Learn/Profile

Run:
pip install -r requirements.txt
streamlit run app.py

Uses the supplied vitamin deficiency dataset.
